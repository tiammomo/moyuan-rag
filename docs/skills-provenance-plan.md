# Skills Provenance Plan

## Goal

Carry install provenance forward into later binding and validation workflows so operators can explain which install event introduced the runtime behavior they are seeing.

## Checklist

- [pending] Add optional install provenance references when operators bind or update a skill from the admin console.
- [pending] Surface provenance badges on robot skill bindings and runtime-active skill payloads.
- [pending] Extend task timelines so provenance-linked binding actions can appear next to install events in one audit trail.
