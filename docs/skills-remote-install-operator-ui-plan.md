# Skills Remote Install Operator UI Plan

## Goal

Make the controlled remote install flow directly operable from the admin console instead of requiring manual API requests.

## Checklist

- [pending] Add an admin-only remote install request form in `/admin/skills`.
- [pending] Surface verification badges and download metadata in the task list without requiring raw JSON inspection.
- [pending] Add an operator smoke test and walkthrough for successful remote install from request to robot binding review.
