# Skills Bootstrap Plan

## Goal

Turn the frozen `skills` design into the first runnable implementation slice inside the repository.

## Checklist

- [pending] Add backend settings for `SKILL_INSTALL_ROOT` and `ENABLE_REMOTE_SKILL_INSTALL`.
- [pending] Implement a local filesystem registry reader for installed skills.
- [pending] Add read-only backend APIs for listing skills and reading skill detail.
- [pending] Add a frontend `/skills` list page and `/skills/[id]` detail page using the read-only APIs.
- [pending] Add robot-to-skill binding schema and backend binding APIs.
- [pending] Add local package install flow and reject remote install unless explicitly enabled.
- [pending] Add tests, operator docs, and README updates for the bootstrap slice.
