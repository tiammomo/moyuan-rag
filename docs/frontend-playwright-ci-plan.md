# Frontend Playwright CI Plan

## Goal

Promote the local Playwright smoke workflow into a repeatable CI or operator automation step with stable inputs and artifact collection.

## Checklist

- [pending] Decide whether the first integration target should be local operator tooling, CI, or both.
- [pending] Define how smoke credentials are provisioned safely outside a developer workstation, without depending on ad hoc local admin accounts.
- [pending] Wire the Playwright smoke summary and screenshots into a stable artifact export path for automated runs.
- [pending] Sync `README.md`, `docs/README.md`, and relevant runbooks with the finalized CI/operator smoke workflow.
