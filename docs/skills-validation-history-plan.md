# Skills Validation History Plan

## Goal

Give teams a lightweight history of validation exports so repeated rollouts can be compared over time instead of relying on one-off copied notes.

## Checklist

- [pending] Define a local history format for exported validation artifacts grouped by install task and rollout date.
- [pending] Surface a small UI hint or operator link for where validation artifacts should live after export.
- [pending] Document how to compare two historical validation exports when investigating regressions across skill versions.
